# Internal KB Assistant & Learned Rules

> This document covers two interrelated features added to Papi Chulo: the **Internal KB Assistant** (team members ask questions in Slack, Papi Chulo answers from the knowledge base) and the **Learned Rules System** (Papi Chulo detects patterns in CS team edits, proposes new response rules, and lets the team manage them from Slack). For the core guest messaging pipeline, see `2026-03-18-1312-architecture.md`.

<!-- Mermaid Color Palette (matches architecture.md)
classDef service fill:#4A90E2,stroke:#2E5C8A,color:#fff
classDef storage fill:#7B68EE,stroke:#5B4BC7,color:#fff
classDef external fill:#F5A623,stroke:#C4841A,color:#fff
classDef decision fill:#F8E71C,stroke:#C7B916,color:#333
classDef event fill:#50C878,stroke:#2D7A4A,color:#fff
classDef error fill:#E74C3C,stroke:#A93226,color:#fff

Legend: blue=service, purple=storage, orange=external, yellow=decision, green=event, red=error
-->

---

## 1. KB Assistant — System Context

The KB Assistant is an internal Q&A tool that runs inside the same Papi Chulo process. Team members @mention the bot in a designated Slack channel; the bot searches the knowledge base and either answers or invites the team to contribute a new answer.

```mermaid
graph LR
    TM([Team Member]) -->|"1. @mention question"| SK[Slack KB Channel]
    SK -->|"2. app_mention event"| PC[Papi Chulo]
    PC -->|"3. search"| KB[("knowledge-base/")]
    PC -->|"4. classify with context"| CL[Claude AI]
    CL -->|"5. answer + source"| PC
    PC -->|"6a. answer in thread"| SK
    PC -->|"6b. don't-know + button"| SK
    TM -->|"7. click Add Answer"| SK
    SK -->|"8. modal submit"| PC
    PC -->|"9. append entry"| KB
    PC -->|"10. confirm + Undo"| SK
    TM -->|"11. click Undo"| SK
    SK -->|"12. undo action"| PC
    PC -->|"13. remove entry"| KB

    classDef service fill:#4A90E2,stroke:#2E5C8A,color:#fff
    classDef storage fill:#7B68EE,stroke:#5B4BC7,color:#fff
    classDef external fill:#F5A623,stroke:#C4841A,color:#fff
    classDef event fill:#50C878,stroke:#2D7A4A,color:#fff

    class PC service
    class KB storage
    class SK,CL external
    class TM event
```

| # | What happens |
|---|---|
| 1 | A team member types `@Papi Chulo what's the WiFi at Hayride 4403?` in the designated KB channel (`SLACK_KB_CHANNEL_ID`) |
| 2 | Slack fires an `app_mention` Socket Mode event to Papi Chulo |
| 3 | `MultiPropertyKBReader.search()` searches the common KB plus the matching property KB (if a property name/address is detected in the question) |
| 4 | The KB context sections and the question are sent to the AI model via OpenRouter |
| 5 | Claude returns a structured JSON result: `{ found, answer, source }` |
| 6a | If the answer was found, Papi Chulo replies in a thread under the original question, with the answer text and a source citation |
| 6b | If the answer was not found, Papi Chulo replies with "I don't have this in my knowledge base" and a **📝 Add Answer** button |
| 7 | Any team member clicks the **Add Answer** button |
| 8 | A modal opens pre-filled with the original question; the team member types the answer and submits |
| 9 | The answer is appended to the correct KB file (`knowledge-base/common.md` or `knowledge-base/properties/{code}.md`) under the `## Team Additions` section. The KB reader picks it up immediately on the next question (reads from disk on every call) |
| 10 | A confirmation is posted in the thread with the added text and a permanent **↩️ Undo** button |
| 11 | Any team member can click **Undo** at any time |
| 12 | Slack fires a `kb_undo_add` action |
| 13 | The exact text that was appended is surgically removed from the KB file; the confirmation message updates to "Entry removed" |

---

## 2. KB Assistant — Question Flow

The full decision path from an @mention arriving to the response being posted.

```mermaid
flowchart TD
    subgraph Guard["Guard Checks"]
        M([app_mention event]) --> CH{Correct channel?}
        CH -->|no| IGN([Ignore silently])
        CH -->|yes| BOT{Bot's own message?}
        BOT -->|yes| IGN2([Ignore silently])
        BOT -->|no| STRIP[Strip @mention from text]
    end

    subgraph Search["KB Search"]
        STRIP --> PROP[Detect property in question]
        PROP --> KBS[Search common KB + property KB]
    end

    subgraph Answer["Claude Answer"]
        KBS --> CLAUDE[Call Claude with question + KB context]
        CLAUDE --> FOUND{answer found?}
    end

    subgraph Response["Thread Response"]
        FOUND -->|yes| ANS[Post answer + source citation in thread]
        FOUND -->|no| DONTKNOW[Post don't-know + Add Answer button in thread]
    end

    classDef event fill:#50C878,stroke:#2D7A4A,color:#fff
    classDef decision fill:#F8E71C,stroke:#C7B916,color:#333
    classDef service fill:#4A90E2,stroke:#2E5C8A,color:#fff

    class M,IGN,IGN2,ANS,DONTKNOW event
    class CH,BOT,FOUND decision
    class STRIP,PROP,KBS,CLAUDE service
```

| # | Detail |
|---|---|
| 1 | Only events in `SLACK_KB_CHANNEL_ID` are processed — all other channels are silently ignored |
| 2 | Messages with a `bot_id` field are skipped to prevent the bot from responding to its own output |
| 3 | `<@UBOT_ID>` mention tokens are stripped from the message text, leaving just the question |
| 4 | `detectPropertyInQuestion()` scans the question against all 16 property names, codes, and addresses in `property-map.json` using the same partial-match logic as the KB reader |
| 5 | `MultiPropertyKBReader.search()` returns matching sections — property-specific results first, then common KB results |
| 6 | Claude receives the system prompt (`KB_ASSISTANT_PROMPT`) and the question + KB context. It must respond in a strict JSON format: `{"found": bool, "answer": string|null, "source": string|null}` |
| 7 | If `found === true`, the answer is posted in a Slack thread with a `Source:` citation line |
| 8 | If `found === false`, the "I don't know" message is posted with the **📝 Add Answer** button. The button's `value` payload embeds the original question and the Slack message timestamp |

---

## 3. KB Assistant — Add Answer & Undo Flow

The path when a team member contributes a new KB entry and optionally reverts it.

```mermaid
flowchart LR
    subgraph Add["Add Answer Flow"]
        BTN([Add Answer button clicked]) --> OPEN[Open modal\nwith question context]
        OPEN --> SUBMIT{Answer submitted?}
        SUBMIT -->|empty| ERR([Modal validation error])
        SUBMIT -->|valid text| RESOLVE[Resolve target KB file]
        RESOLVE --> PROP2{Property in question?}
        PROP2 -->|yes| PROPFILE[("knowledge-base/properties/{code}.md")]
        PROP2 -->|no| COMMON[("knowledge-base/common.md")]
        PROPFILE --> FORMAT[formatKBEntry\nClaude call]
        COMMON --> FORMAT
        FORMAT --> APPEND[appendToKB]
        APPEND --> DEL[Delete original\ndont-know message]
        DEL --> CONFIRM([Post fresh confirmation\n+ Undo button])
    end

    subgraph Undo["Undo Flow"]
        UBTN([Undo button clicked]) --> REMOVE[undoAppend]
        REMOVE --> FOUND2{Entry found in file?}
        FOUND2 -->|yes| UPDATE([Update message in-place\n↩️ Entry removed])
        FOUND2 -->|no| EPHEMERAL([Ephemeral: already removed])
    end

    classDef event fill:#50C878,stroke:#2D7A4A,color:#fff
    classDef decision fill:#F8E71C,stroke:#C7B916,color:#333
    classDef service fill:#4A90E2,stroke:#2E5C8A,color:#fff
    classDef storage fill:#7B68EE,stroke:#5B4BC7,color:#fff
    classDef error fill:#E74C3C,stroke:#A93226,color:#fff

    class BTN,UBTN,CONFIRM,UPDATE,EPHEMERAL event
    class SUBMIT,PROP2,FOUND2 decision
    class OPEN,RESOLVE,FORMAT,APPEND,DEL,REMOVE service
    class PROPFILE,COMMON storage
    class ERR error
```

| # | Detail |
|---|---|
| 1 | The **Add Answer** button value carries `{ question, threadTs }` — the modal's `private_metadata` stores `{ question, channelId, threadTs, messageTs }` so the submission handler knows both where to post the confirmation and which "don't know" message to delete |
| 2 | Empty answer text is caught by a Slack validation error response — the modal stays open with an inline error |
| 3 | `resolveKBFilePath()` detects a property in the original question and maps it to the correct KB file. If no property is found, the answer goes to `common.md` |
| 4 | `appendToKB()` atomically writes to the KB file (write to `.tmp` then `rename()`). It creates a `## Team Additions` section if one doesn't exist yet, or inserts the new entry directly after the existing header |
| 5 | The answer is first passed to `formatKBEntry()` (a Claude call) which returns a properly structured entry starting with `### Title`. `appendToKB()` detects the `###` prefix and skips adding its own heading, preventing double `###` |
| 6 | After appending, the original "don't know" message is **deleted** (`chat.delete`) and a fresh confirmation is **posted** as a new message (`chat.postMessage`) — not updated in place |
| 7 | The Undo button's `value` payload carries `{ filePath, appendedText }` — the exact string that was inserted, so it can be surgically removed |
| 8 | `undoAppend()` does an exact string replacement, then collapses any triple blank lines left behind. Atomic write using the same tmp+rename pattern |
| 9 | If the entry is already gone (manual edit, or double-click), an ephemeral "already removed" message is shown only to the clicking user |

---

## 4. KB Assistant — Answer Feedback & Correction

Every KB answer message includes two feedback buttons: **✅ Correct** and **❌ Not Quite**. These let the team signal whether Papi Chulo's answer was right, and submit a correction if it wasn't.

```mermaid
flowchart LR
    ANS([KB answer posted\nwith Correct + Not Quite buttons])

    subgraph CP["Correct Path"]
        ANS --> C1[kb_confirm_answer action]
        C1 --> C2[recordFeedback\ntype: correct]
        C2 --> C3[chat.update in place\nbuildKBConfirmedBlocks]
        C3 --> C4([Buttons removed\nConfirmed by user shown])
    end

    subgraph NQ["Not Quite Path"]
        ANS --> N1[kb_incorrect_answer action\nack first — 3s window]
        N1 --> N2[Open correction modal\nbuildKBCorrectionModal\npre-filled with Q + A]
        N2 --> N3{Correction submitted?}
        N3 -->|empty| N4([Validation error\nmodal stays open])
        N3 -->|valid text| N5[formatKBEntry\nClaude call]
        N5 --> N6[appendToKB\nto same filePath]
        N6 --> N7[recordFeedback\ntype: incorrect]
        N7 --> N8[chat.update in place\nbuildKBCorrectedBlocks]
        N8 --> N9([Correction saved\n+ Undo button shown])
    end

    classDef event fill:#50C878,stroke:#2D7A4A,color:#fff
    classDef decision fill:#F8E71C,stroke:#C7B916,color:#333
    classDef service fill:#4A90E2,stroke:#2E5C8A,color:#fff
    classDef error fill:#E74C3C,stroke:#A93226,color:#fff

    class ANS,C4,N4,N9 event
    class N3 decision
    class C1,C2,C3,N1,N2,N5,N6,N7,N8 service
```

| # | Detail |
|---|---|
| 1 | Both buttons are built into every answer message by `buildKBAnswerBlocks()`. Their `value` payloads carry `{ question, answer, source, filePath }` (Correct) and `{ question, answer, filePath }` (Not Quite) |
| 2 | **Correct path**: `recordFeedback({ type: 'correct', question, aiAnswer, filePath, userId })` is called fire-and-forget. No KB file changes — the answer was already correct |
| 3 | The answer message is updated in place via `chat.update` with `buildKBConfirmedBlocks`, which removes the buttons and shows "✅ Confirmed by @user" |
| 4 | **Not Quite path**: `ack()` is called first, before any async work — the Slack trigger_id expires in 3 seconds and the modal won't open if ack is delayed |
| 5 | The correction modal (`buildKBCorrectionModal`) is pre-filled with the original question and the original AI answer so the team member can see what they're correcting |
| 6 | Empty correction text returns a Slack validation error (`response_action: 'errors'`) — the modal stays open with an inline message |
| 7 | Valid correction: `formatKBEntry(question, correctionText)` runs a Claude call to produce a properly structured `### Title\n...` entry, then `appendToKB()` writes it to the same `filePath` the original answer came from |
| 8 | `recordFeedback({ type: 'incorrect', correction: correctionText, ... })` is called after the KB write — the correction text is stored alongside the original AI answer for future analysis |
| 9 | The original answer message is updated in place with `buildKBCorrectedBlocks`, which shows the saved correction text and an Undo button (same `kb_undo_add` action as the Add Answer flow) |

---

## 5. KB Assistant — Feedback Store

`data/kb-feedback.json` accumulates every Correct and Not Quite signal the team submits. Nothing reads it today — it's a data collection layer that enables future quality reporting.

```mermaid
graph LR
    subgraph Schema["data/kb-feedback.json"]
        ROOT["{ entries: KBFeedbackEntry[], version: number }"]
        ROOT --> E1["entry: { id, type, question,\naiAnswer, correction?, filePath,\nuserId, timestamp }"]
    end

    subgraph Write["Write Path"]
        REC[recordFeedback] --> LOAD[getCachedFile\nload from disk if cache null]
        LOAD --> BUILD[Build new entry\nid: UUID, timestamp: ISO 8601]
        BUILD --> TMP[("Write to kb-feedback.tmp.json")]
        TMP --> REN[rename to kb-feedback.json]
        REN --> UPD[Update in-memory cache]
    end

    subgraph Read["Read Path"]
        LF[loadFeedback] --> CACHE{cachedFile null?}
        CACHE -->|yes| DISK[readFromDisk]
        CACHE -->|no| RET([Return cached entries])
        DISK --> RET
    end

    classDef service fill:#4A90E2,stroke:#2E5C8A,color:#fff
    classDef storage fill:#7B68EE,stroke:#5B4BC7,color:#fff
    classDef decision fill:#F8E71C,stroke:#C7B916,color:#333
    classDef event fill:#50C878,stroke:#2D7A4A,color:#fff

    class REC,LOAD,BUILD,REN,UPD,LF,DISK service
    class ROOT,E1,TMP storage
    class CACHE decision
    class RET event
```

| Detail | Explanation |
|---|---|
| **File path** | `data/kb-feedback.json` by default. Override with the `KB_FEEDBACK_FILE` env var |
| **Schema** | `{ entries: KBFeedbackEntry[], version: number }`. `version` is always `1` for now — reserved for future migrations |
| **Entry fields** | `id` (UUID via `crypto.randomUUID()`), `type` ("correct" or "incorrect"), `question`, `aiAnswer`, `correction` (only present on incorrect entries), `filePath`, `userId`, `timestamp` (ISO 8601) |
| **In-memory cache** | Module-level `cachedFile` variable. Starts as `null`. First call to `recordFeedback()` or `loadFeedback()` reads from disk and populates the cache. All subsequent calls use the cache — no repeated disk I/O |
| **Atomic write** | `recordFeedback()` writes to `kb-feedback.tmp.json` then calls `rename()` to the final path. Prevents partial writes from corrupting the file if the process crashes mid-write |
| **`data/` is gitignored** | The file is not committed to git. It's created automatically on first feedback submission. `loadFeedback()` returns `[]` gracefully if the file doesn't exist |
| **What it's used for today** | Data collection only. The file accumulates entries but nothing reads it in the current pipeline |
| **What it enables** | Future quality reporting ("Papi Chulo was correct X% of the time"), pattern detection across incorrect answers, and targeted prompt improvements |

---

## 6. Knowledge Base File Structure

How the KB files are organized and how the Team Additions section looks on disk.

```mermaid
graph TD
    subgraph KBDir["knowledge-base/"]
        CMN[("common.md\nShared policies + 16 Q&A scenarios")]
        MAP[("property-map.json\n16 entries mapping names → files")]
        subgraph Props["properties/"]
            P1[("4403-hay.md")]
            P2[("3401-bre.md")]
            P3[("... 14 more")]
        end
    end

    subgraph CMNFile["common.md (example structure)"]
        H1["## WiFi\n..."]
        H2["## Check-in\n..."]
        H3["## Team Additions\n(created on first Add Answer)"]
        TA1["### WiFi at Hayride 4403\nPassword is XYZ\n_Added via Slack on 2026-03-19_"]
        TA2["### Late check-out cost\n$50 per hour\n_Added via Slack on 2026-03-20_"]
        H3 --> TA1
        H3 --> TA2
    end

    classDef storage fill:#7B68EE,stroke:#5B4BC7,color:#fff
    class CMN,MAP,P1,P2,P3 storage
    class H1,H2,H3,TA1,TA2 storage
```

| Detail | Explanation |
|---|---|
| **common.md** | Loaded for every question. Contains shared policies, 16 pre-built Q&A scenarios, a property quick-reference table, and the service provider directory |
| **properties/{code}.md** | Loaded only when a property is detected in the question. Contains WiFi, amenities, house rules, fees, and per-unit details |
| **## Team Additions** | Auto-created at the bottom of the target file on first use. All KB assistant additions go here. The KB reader's section parser picks up `### Entry` sub-headers as individual searchable sections |
| **Atomic write** | `appendToKB()` and `undoAppend()` write to `{file}.tmp` then `rename()` to the target, preventing partial writes from corrupting the KB |
| **Live updates** | `MultiPropertyKBReader` reads from disk on every call — no caching. A Team Additions entry is searchable immediately after it's written |

---

## 7. Learned Rules System — Weekly Lifecycle

Papi Chulo monitors how the CS team edits its draft responses and proposes rules to improve future drafts. The full cycle runs automatically every Friday at 12:00 PM CST.

```mermaid
graph LR
    subgraph Capture["Capture (every action)"]
        A([CS edits a response]) --> LOG[Append to logs/actions.jsonl\noriginalDraft + editedText]
    end

    subgraph Analyze["Analyze (Friday 12 PM CST)"]
        SCHED[Croner scheduler fires] --> EA[runWeeklyAnalysis]
        EA --> READ[Read logs/actions.jsonl\nfilter edit entries]
        READ --> PAT[Detect 4 patterns\ngreeting · signoff · shortened · lengthened]
        PAT --> MERGE[Merge with data/learned-rules.json\nskip already-proposed/confirmed/rejected]
        MERGE --> SAVE[Save rules file]
        SAVE --> MD[Generate logs/weekly-recap-YYYY-MM-DD.md]
    end

    subgraph Notify["Notify (same Friday)"]
        SAVE --> POST[Post Weekly Recap to Slack\nProposed rules with Approve/Reject buttons]
    end

    subgraph Act["Act (CS team, any time)"]
        POST --> APR([Approve → status: confirmed])
        POST --> REJ([Reject → status: rejected])
        POST --> ADD([Add Custom Rule → status: confirmed])
        POST --> REM([Remove Rule → status: rejected])
        APR --> INVAL[invalidateCache]
        REJ --> INVAL
        ADD --> INVAL
        REM --> INVAL
    end

    subgraph Use["Use (next Claude call)"]
        INVAL --> RULES[getConfirmedRules\ncalled per classification]
        RULES --> PROMPT[Injected into SYSTEM_PROMPT\nas LEARNED RULES FROM CS TEAM FEEDBACK]
    end

    classDef event fill:#50C878,stroke:#2D7A4A,color:#fff
    classDef service fill:#4A90E2,stroke:#2E5C8A,color:#fff
    classDef storage fill:#7B68EE,stroke:#5B4BC7,color:#fff

    class A,APR,REJ,ADD,REM event
    class SCHED,EA,READ,PAT,MERGE,POST,INVAL,RULES service
    class LOG,SAVE,MD,PROMPT storage
```

| # | What happens |
|---|---|
| 1 | Every time a CS team member edits a draft before sending, the audit logger writes both the `originalDraft` (Claude's suggestion) and `editedText` (what was actually sent) to `logs/actions.jsonl` |
| 2 | Every Friday at 12:00 PM CST, the `croner` scheduler fires `runWeeklyAnalysis()` (`0 12 * * 5`, `America/Chicago` timezone) |
| 3 | The analyzer reads all edit entries from the JSONL file and detects four patterns: greeting removed, sign-off removed, message shortened >30%, message lengthened >50% |
| 4 | Patterns that appear at least twice (frequency ≥ 2) are proposed as new rules. Patterns already in `data/learned-rules.json` as proposed, confirmed, or rejected are skipped |
| 5 | The merged rule set is saved atomically to `data/learned-rules.json` (tmp + rename) |
| 6 | A markdown recap is generated at `logs/weekly-recap-YYYY-MM-DD.md` as a permanent record |
| 7 | The Weekly Recap message is posted to the guest approval Slack channel (`SLACK_CHANNEL_ID`) |
| 8 | Each proposed rule has Approve and Reject buttons; the message also has Add Custom Rule and Remove Rule buttons |
| 9 | On any button action, `invalidateCache()` is called so the next classification uses the updated rule set — no restart required |
| 10 | `getConfirmedRules()` is called inside `callClaude()` on every classification. Because it reads from the in-memory cache (which was just invalidated), new rules take effect on the very next guest message |

---

## 8. Learned Rules — Slack Recap Interactions

The five actions the CS team can take from the weekly recap message.

```mermaid
flowchart LR
    MSG([Weekly Recap message in Slack])
    MSG --> ACT{Which button?}

    subgraph AP2["Approve Rule"]
        A1[Parse ruleId] --> A2[updateRule: status confirmed]
        A2 --> A3[invalidateCache]
        A3 --> A4([Replace buttons with ✅ Confirmed by @user])
    end

    subgraph RJ2["Reject Rule"]
        R1[Parse ruleId] --> R2[updateRule: status rejected]
        R2 --> R3[invalidateCache]
        R3 --> R4([Replace buttons with ❌ Rejected by @user])
    end

    subgraph ADD2["Add Custom Rule"]
        C1[Open modal\nPattern + Correction fields] --> C2[addRule: status confirmed]
        C2 --> C3[invalidateCache]
        C3 --> C4([Post confirmation in channel])
    end

    subgraph REM2["Remove Rule"]
        D1[Open modal\nCheckboxes for each confirmed rule] --> D2[updateRule × N: status rejected]
        D2 --> D3[invalidateCache]
        D3 --> D4([Post confirmation in channel])
    end

    ACT -->|Approve| A1
    ACT -->|Reject| R1
    ACT -->|Add Custom Rule| C1
    ACT -->|Remove Rule| D1

    classDef event fill:#50C878,stroke:#2D7A4A,color:#fff
    classDef decision fill:#F8E71C,stroke:#C7B916,color:#333
    classDef service fill:#4A90E2,stroke:#2E5C8A,color:#fff

    class MSG,A4,R4,C4,D4 event
    class ACT decision
    class A1,A2,A3,R1,R2,R3,C1,C2,C3,D1,D2,D3 service
```

| Action | What it does | Takes effect |
|---|---|---|
| **Approve** | Sets rule `status: confirmed`. `invalidateCache()` forces reload on next classification. Recap buttons replaced with status text (idempotent — double-clicks are safe) | Immediately — next guest message |
| **Reject** | Sets rule `status: rejected`. Prevents the same pattern from being re-proposed in future weekly runs | Immediately |
| **Add Custom Rule** | Modal with two fields: "What does the AI do wrong?" (pattern) and "How should it respond instead?" (correction). Goes directly to `confirmed` — no approval step needed | Immediately |
| **Remove Rule** | Modal with checkboxes listing all confirmed rules. Selected rules set to `rejected`, deactivating them. Does not delete the entry — rejected rules prevent re-proposal | Immediately |

---

## 9. Rules Store — Persistence & Hot-Reload

How `data/learned-rules.json` is managed and how confirmed rules reach Claude without a service restart.

```mermaid
flowchart TD
    subgraph File["data/learned-rules.json"]
        SCHEMA["{ rules: LearnedRule[], lastAnalyzed: string|null, version: number }"]
    end

    subgraph Cache["In-Memory Cache"]
        CACHED["cachedFile: LearnedRulesFile | null"]
    end

    subgraph HotReload["Hot-Reload Mechanism"]
        INV[invalidateCache\nsets cachedFile = null] --> NEXT[Next getConfirmedRules call\nre-reads from disk]
        NEXT --> RULES[Confirmed rules injected\ninto Claude system prompt]
    end

    subgraph Write["Atomic Write Pattern"]
        TMP[("Write to {file}.tmp")] --> REN["rename() to {file}.json"]
        REN --> UPD[Update in-memory cache]
    end

    File -->|"on cache miss"| CACHED
    CACHED -->|"getConfirmedRules()"| RULES
    Write -->|"saveRules / updateRule / addRule"| File

    classDef service fill:#4A90E2,stroke:#2E5C8A,color:#fff
    classDef storage fill:#7B68EE,stroke:#5B4BC7,color:#fff

    class INV,NEXT,RULES,TMP,REN,UPD service
    class File,CACHED storage
```

| Detail | Explanation |
|---|---|
| **Cache** | `rules-store.ts` holds `cachedFile` at module level. On first call, it reads `data/learned-rules.json` and caches the result. Subsequent calls use the cache — no repeated disk I/O per guest message |
| **Hot-reload** | Any Slack button action (approve, reject, add, remove) calls `invalidateCache()` after mutating the file. The next `getConfirmedRules()` call re-reads from disk — new rules take effect on the next guest classification without restarting the service |
| **Atomic write** | All writes go through `saveRules()`, which writes to `{file}.tmp` then calls `rename()`. This prevents partial writes from corrupting the file (e.g., if the process crashes mid-write) |
| **Rule statuses** | `proposed` — detected by analyzer, pending CS team review. `confirmed` — approved by CS team, injected into Claude prompt. `rejected` — dismissed by CS team, excluded from prompts and excluded from re-proposal |
| **`data/` is gitignored** | `data/learned-rules.json` is not committed to git. It is created automatically on first use. `loadRules()` returns `[]` gracefully if the file doesn't exist |
| **Weekly schedule** | The weekly recap runs every Friday at 12:00 PM CST via a cron job in `scheduler.ts`. It does not run on service restart — restarts post a simple online announcement to Slack instead |

---

## 10. Conversation Summary — Deterministic Fallback

Every follow-up guest message now always shows conversation context in Slack, regardless of whether Claude returns a summary.

```mermaid
flowchart LR
    subgraph Fetch["Thread Fetch"]
        GT[getThread from Hostfully] --> MSGS[Last 30 messages\nslice -30]
        MSGS --> FMT["Format: [GUEST]: text\n[PROPERTY_MANAGER]: text\n[SYSTEM]: text"]
    end

    subgraph Claude["Claude Classification"]
        FMT -->|"conversationHistory"| CL[Claude]
        CL --> CS{conversationSummary\nreturned?}
    end

    subgraph Fallback["Fallback"]
        CS -->|"null or empty string"| HIST{conversationHistory\nnon-empty?}
        HIST -->|"yes"| BUILD[buildFallbackSummary\nfilter SYSTEM · humanize labels · cap 400 chars]
        HIST -->|"no"| NULL([null — first message])
        CS -->|"non-empty string"| USE([Use Claude's summary as-is])
        BUILD --> RES([resolvedConversationSummary])
    end

    classDef event fill:#50C878,stroke:#2D7A4A,color:#fff
    classDef decision fill:#F8E71C,stroke:#C7B916,color:#333
    classDef service fill:#4A90E2,stroke:#2E5C8A,color:#fff

    class NULL,USE,RES event
    class CS,HIST decision
    class GT,MSGS,FMT,CL,BUILD service
```

| Detail | Explanation |
|---|---|
| **30 messages** | Thread fetch uses `.slice(-30)` — up from the original 5. Guests often send multiple consecutive messages; 5 was insufficient for a full picture |
| **Fallback triggers on `\|\|`** | Uses `claudeSummary \|\| fallback` (not `??`). This catches both `null` and empty string `""` — Claude occasionally returns an empty string instead of null |
| **`buildFallbackSummary()`** | Filters out `[SYSTEM]` messages (booking confirmations are noise), maps `[GUEST]`/`[TRAVELER]` → `Guest:` and `[PROPERTY_MANAGER]` → `Host:`, caps output at 400 characters with `…` |
| **Always guaranteed** | If `conversationHistory` is non-empty and Claude returns null/empty, the CS team always sees "📋 Conversation so far" in Slack — even without a natural-language summary |
| **First message** | If `conversationHistory` is empty (first message in the thread), both Claude's summary and the fallback return null. The "📋 Conversation so far" section is correctly omitted |

---

## Setup Checklist

Before the KB Assistant is operational, four manual steps are required in the Slack API dashboard and environment config.

| Step | What to do |
|---|---|
| 1. OAuth Scope | In [api.slack.com](https://api.slack.com/apps) → OAuth & Permissions → Bot Token Scopes → add `app_mentions:read` |
| 2. Event Subscription | In the Slack app settings → Socket Mode → Event Subscriptions → add `app_mention` event |
| 3. Environment Variable | Add `SLACK_KB_CHANNEL_ID=<channel-id>` to `.env`. Get the channel ID from Slack: right-click the channel → View channel details → copy the ID at the bottom |
| 4. Invite the bot | In Slack, go to the KB channel and type `/invite @Papi Chulo` |

The Learned Rules system and the Conversation Summary fallback require no additional setup — they activate automatically when the service restarts.
